require_relative '../spec_helper'

describe 'signed urls', type: :integration do
  with_reset_sandbox_before_each

  context 'when the enable_signed_urls property is configured' do
    it 'should deploy an instance with no blobstore credentials' do

    end
  end
end
